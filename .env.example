# Скопируйте в файл .env в корне проекта (файл .env в git не попадает).
# На Netlify: Site configuration → Environment variables — те же три имени.

AUTH_USER=daogreen
AUTH_PASS=замените-на-сильный-пароль
AUTH_SECRET=случайная-строка-минимум-32-символа-буквы-и-цифры
