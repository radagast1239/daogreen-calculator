/** Пример — скопируйте в preview-config.js и измените под себя. */
(function (g) {
  g.DG_PREVIEW_CONFIG = {
    appView: 'channels',
    facility: 'greenhouse',
    cv: 'aficion',
    temp: 26,
    lighting: true,
    day: 24,
    nch: 15,
    density: 45,
    georgyMode: false
  };
})(typeof window !== 'undefined' ? window : global);
